version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12.x
    commands:
      - echo Insatlling NPM Packages and wget Enviorment File
  pre_build:
    commands:
      - npm i
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build started on `date`

artifacts:
  files:
    - "**/*"
